<p id="notice"><%= notice %></p>

<article class="page">

  <header>
    <h1><%= @page.title %></h1>
  </header>

  <div class="body">
    <%= raw @page.body %>
  </div>

  <footer>
    <%= link_to 'Edit', edit_page_path(@page) %> |
    <%= link_to 'Back', pages_path %>
  </footer>

</article>
